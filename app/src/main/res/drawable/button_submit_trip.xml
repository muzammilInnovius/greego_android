<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
<item>
    <shape>
        <stroke
            android:width="1dp"
            android:color="@color/btn_submit" />

        <solid android:color="@color/btn_submit" />

        <gradient
            android:angle="180"
            android:centerColor="@color/btn_submit"
            android:endColor="@color/btn_submit"
            android:startColor="@color/btn_submit" />
        <corners android:radius="0dp" />

        <corners android:radius="180dp" />
    </shape>
</item>
</layer-list>