<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <stroke
                android:width="1dp"
                android:color="@color/on_trip_bg" />

            <solid android:color="@color/on_trip_bg" />

            <gradient
                android:centerColor="@color/on_trip_bg"
                android:endColor="@color/on_trip_bg"
                android:startColor="@color/on_trip_bg" />
            <corners android:radius="0dp" />

            <corners android:radius="10dp" />
        </shape>
    </item>
</layer-list>